
<VirtualHost *:80>
        ServerAdmin me@mydomain.com
        ProxyRequests Off
        Order deny,allow
        Allow from all
 
        ProxyPass / https://localhost:4000
        ProxyPassReverse / https://localhost:4000
 
</VirtualHost>

